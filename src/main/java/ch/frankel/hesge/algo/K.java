package ch.frankel.hesge.algo;

import ch.frankel.hesge.algo.model.Chef;

import java.util.List;
import java.util.function.Function;
import java.util.stream.Stream;

public class K implements Function<Stream<Chef>, List<Chef>> {

    /**
     * Reprendre la question C en utilisant l'API Stream.
     */
    @Override
    public List<Chef> apply(Stream<Chef> chefs) {
        return null;
    }
}
